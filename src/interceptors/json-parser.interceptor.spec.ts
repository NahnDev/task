import { JsonParserInterceptor } from './json-parser.interceptor';

describe('JsonParserInterceptor', () => {
  it('should be defined', () => {
    expect(new JsonParserInterceptor()).toBeDefined();
  });
});
